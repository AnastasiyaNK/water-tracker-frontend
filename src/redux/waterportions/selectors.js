// // import { createSelector } from '@reduxjs/toolkit';

// export const selectWaterPortions = state => state.waterportions;
// export const selectLoading = state => state.waterportions.isLoading;
// export const selectError = state => state.waterportions.error;
// export const selectFilter = state => state.filter;

// // export const selectVisibleContacts = createSelector(
// //   [selectContacts, selectFilter],
// //   (contacts, filter) => {
// //     return contacts.list.filter(contact =>
// //       contact.name.toLowerCase().includes(filter.toLowerCase())
// //     );
// //   }
// // );
